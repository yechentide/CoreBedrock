# 0x76 LegacyChunkVersion

## Key

```txt
[chunkX][chunkZ][dimension?][0x76]
```

## Value

Legacy version value. Described as 1 byte.

## Status

The version record moved to `0x2C`. For older worlds, check `0x76` as a fallback.
