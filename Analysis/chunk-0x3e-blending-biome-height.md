# 0x3E BlendingBiomeHeight

## Key

```txt
[chunkX][chunkZ][dimension?][0x3E]
```

## Value

Appears unused, but should be treated as a migration marker that must not be removed.

## Notes

Public references vary in naming around `0x3E` / `0x3F`. Treat the raw key byte as authoritative, and treat display names as version-specific annotations.
